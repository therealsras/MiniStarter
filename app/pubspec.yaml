name: app
description: "simple mini starter app"
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.2.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.2
  dio: ^5.4.1
  flutter_dotenv: ^5.1.0
  bot_toast: ^4.1.3
  jwt_decoder: ^2.0.1
  shared_preferences: ^2.2.2
  flutter_riverpod: ^2.4.10
  google_fonts: ^6.2.0
  get_it: ^7.6.7
  json_annotation: ^4.8.1
  desktop_window: ^0.4.0
  go_router: ^13.2.0
  flutter_svg: ^2.0.10+1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true